[project]
name = "reconstruction"
version = "0.1.0"
description = "COLMAP 3D Reconstruction Pipeline for Estaciona AI"
readme = "README.md"
requires-python = ">=3.11, <3.13"
dependencies = [
    "tqdm>=4.67.3",
    "numpy>=1.26.0",
]

[project.scripts]
reconstruction = "main:main" 
